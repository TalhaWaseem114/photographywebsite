import JournalClient from './JournalClient';

export const metadata = {
  title: "Journal — Hanzala Photography",
  description: "A chronological visual journal compiling in-depth photography essays, creative narratives, and daily technical camera logs."
};

export default function Journal() {
  return <JournalClient />;
}
