import ContactClient from './ContactClient';

export const metadata = {
  title: "Contact — Hanzala Photography",
  description: "Get in touch for photography bookings, collaborative inquiries, or prints. Typical response within 24 hours."
};

export default function Contact() {
  return <ContactClient />;
}

